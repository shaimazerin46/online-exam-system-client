

const WebButton = ({text}) => {
    return (
        <div>
            <button className="btn bg-[#4F959D] border-0 rounded-xl text-white">{text}</button>
        </div>
    );
};

export default WebButton;